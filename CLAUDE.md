# Project: Notebook

개인 학습 노트북 및 코드 예제 프로젝트.

## Sub-Agent Routing Rules

### Parallel Dispatch (병렬 실행)

다음 조건을 **모두** 만족할 때 병렬 서브에이전트를 사용한다:

- 독립적인 태스크가 **3개 이상**
- 태스크 간 **공유 상태(변수, 전역 설정)가 없음**
- 수정 대상 **파일 경계가 겹치지 않음** (같은 파일을 동시에 수정하지 않음)
- 각 태스크의 **입출력이 다른 태스크에 의존하지 않음**

예시: 서로 다른 TIL 문서 분석, 독립된 코드 모듈 리뷰, 여러 디렉터리 탐색

### Sequential Dispatch (순차 실행)

다음 중 **하나라도** 해당하면 순차 실행한다:

- 태스크 A의 결과가 태스크 B의 입력으로 필요
- 동일 파일을 여러 태스크가 수정해야 함
- 태스크 스코프가 불명확하여 충돌 가능성이 있음
- 이전 태스크의 성공/실패에 따라 후속 태스크가 달라짐

예시: 코드 분석 → 리팩터링, 설정 파일 수정 → 검증

### Background Dispatch (백그라운드 실행)

다음 조건에 해당하면 백그라운드로 실행한다:

- **리서치/분석** 태스크로 결과가 현재 작업을 차단하지 않음
- 코드베이스 전체 탐색 등 **시간이 오래 걸리는** 읽기 전용 작업
- 결과를 **나중에 참조**해도 되는 보조 태스크

예시: 프로젝트 전체 구조 분석, 관련 문서 검색, 의존성 조사

## Domain Parallel Patterns

프로젝트 도메인별 에이전트 역할 정의:

- **TIL 문서** (`TIL/`): 문서 분석, 내용 검토, 요약 — 문서별 병렬 가능
- **코드 예제** (`code/`): 코드 리뷰, 구조 분석, 리팩터링 — 모듈별 병렬 가능
- **리소스** (`resourse/`): 리소스 파일 확인, 정리 — 독립 병렬 가능
- **프로젝트 메타** (루트: `history.md`, `CLAUDE.md`): 설정/문서 관리 — 파일별 병렬 가능

### 병렬 실행 금지 조합

- `history.md` 수정 + TIL 문서 이동 (히스토리 참조 불일치 가능)
- 같은 `code/` 하위 모듈 내 여러 파일 동시 수정
